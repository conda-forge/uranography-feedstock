{% set name = "uranography" %}
{% set version = "1.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/uranography-{{ version }}.tar.gz
  sha256: 2c12c306eab852ed59272764b529b473549fa78974fb412b4c73c49afc9d316c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - pandas
    - astropy-base >=5.3
    - healpy
    - healsparse
    - bokeh >=3.1.1
    - panel >=1.1.0
    - colorcet
    - jupyter
    - nbconvert
    - selenium

test:
  imports:
    - uranography
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/lsst/uranography
  summary: Tools for visualization of the celestial sphere.
  doc_url: https://uranography.lsst.io
  license: GPL-3.0-or-later
  license_file:
    - LICENSE
    - COPYRIGHT

extra:
  recipe-maintainers:
    - rhiannonlynne
    - ehneilsen
